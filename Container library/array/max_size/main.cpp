/**
 * ClassName: ${NAME}
 * package: ${PACKAGE_NAME}
 * Description:
 * @Author: innno
 * @Create: 2024/2/27 - 21:54
 * @Version: v1.0
 */
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false), cin.tie(nullptr), cout.tie(nullptr);
    array<char, 10> p;
    array<long, 10> q;
    cout << uppercase
            << "p.max_size() = " << dec << p.max_size() << " = 0x"
            << hex << p.max_size() << "\n"
            << "q.max_size() = " << dec << q.max_size() << " = 0x"
            << hex << q.max_size() << "\n";
    return 0;
}
